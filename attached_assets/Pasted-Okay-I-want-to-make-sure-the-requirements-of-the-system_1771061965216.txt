Okay, I want to make sure the requirements of the system are clear, and so we have an architecture that supports this. We've built a new type of protocol in JSON called server-driven UI, and the code base is called flows or journeys. Ideally, I'd like to rename all of it to flows if possible, to make it clearer and more consistent, so we can tidy up our architecture. And the idea is that we use a flow editor to edit some screens, and screens are just a bunch of JSON definitions of screens. And we can also have a prompt which references those screens and navigates those screens with tool calls. Now, each one of those screens can have elements on them, and one of those elements is an agent card. And we want to make sure we maintain the current screen and the navigation state as we navigate back and forth through the app via voice, because a voice agent will be speaking as we view some of these screens. But the agent card is dynamic, so the way we decide whether screens elements stay on screen is by defining them and defining conditions to show and hide them. And those conditions can be changed based on module data or state data, and that state data needs to persist throughout the session of that flow. And the idea is that when we call a tool call to capture some sort of input or capture goals or capture motivation, that we save some data that modifies the state that triggers a condition which reveals and shows elements, so we can create all sorts of dynamic animations. And it's really important that this works really consistently and effectively. I want to be able to edit the screen JSON, save it, publish it, and use it straight away and see the changes live. So I wanna make sure the database is working correctly. This is all built using Replit, so it needs to understand the Replit architecture. And I wanna be able to edit the prompt, and the prompts needs to understand the concept of screens a little bit and understand what screen it's currently on. Now, we have some issues in the current app where we show an agent message, and just before, if a member responds, it flashes up the agent message again whilst it's waiting for the next message to come in. So we need to make sure that we only show one agent message at a time, and if we do capture input and we're still on the same screen, the conditions mean that the UI that we use to capture that input stays on screen, so whether that's set goals or motivations or anything like that, we wanna keep the UI on screen using these conditions. So we need to evaluate the architecture to make sure that this works robustly. And yeah, that's it.